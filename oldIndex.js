// eslint-disable-next-line no-lone-blocks
{
  /* <div className='flex min-h-screen flex-col items-center justify-center py-2'>
      <Head>
        <title>Create Next App</title>
        <link rel='icon' href='/favicon.ico' />
      </Head>
      <div className='grid grid-cols-2 md:grid-cols-3 lg:grid-cols-4 gap-x-8 gap-y-16 px-4 w-full justify-items-center'>
        {characters.map((character) => (
          <div key={character.id}>
            <CharInfo character={character} />
          </div>
        ))}
      </div>
    </div>

export const getServerSideProps: GetServerSideProps = async (_context) => {
  const { data } = await axios.get<RickMortyResults>(
    'https://rickandmortyapi.com/api/character'
  )

  const { results }: RickMortyResults = data

  return {
    props: {
      characters: results
    }
  }
} */
}
